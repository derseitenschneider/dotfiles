<?php
/**
 * Plugin header — add these two lines to your main plugin file's docblock.
 *
 * Replace {slug} with your plugin's text-domain (conventionally the plugin
 * directory name, e.g. "my-plugin").
 *
 * Text Domain: {slug}
 * Domain Path: /languages
 *
 * The /languages path is relative to the plugin directory. WordPress's
 * Textdomain Registry uses these two headers to know where to look for .mo
 * files when a string in your domain is first requested.
 */
